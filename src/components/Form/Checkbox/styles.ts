import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  align-items: center;

  input {
    transform: scale(1.2);
    cursor: pointer;
  }

  label {
    margin-left: 0.6rem;
  }
`;